# Vue 3 + TypeScript + Vite

This template should help get you started developing with Vue 3 and TypeScript in Vite. 
The template uses Vue 3 options API

## Run project

```sh
npm run dev
```
Running the project also runs the lint which is included in vite.config as an extension

## Build the project

```sh
npm run build
```